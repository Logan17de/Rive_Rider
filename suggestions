Veyra milestone suggestions
===========================

Current milestone
-----------------
- M0–M5 foundation, interaction and workspace UX work is independently VERIFIED.
- Active work is now `milestone.md` M6 — Multi-Artboard, Components & Project Graph, mapping to master `plan.md` M2.
- Keep M6 focused on stable artboard/component/instance identity, component evaluation/runtime isolation, cross-artboard dependency/ownership and canonical UI/AI control parity.

Queued work after M6
--------------------
1. Resume the master `plan.md` sequence with View Models / Data Binding / Property Groups / enums / converters / lists.
2. Continue into full layered state-machine parity and later Rive feature families in plan order.
3. Add path-topology commands (`addVertex` / `removeVertex`) plus a mechanical UI-to-command parity audit when it fits the active feature boundary.
4. Revisit the paused headless Node API/CLI only after the browser/editor contracts being reused are settled.

Guardrails
----------
- `milestone.md` remains the only active implementation milestone.
- Future work stays here until the current milestone is independently accepted.
- Stable IDs and human display names remain separate contracts.
- UI convenience must not introduce a second mutation model or bypass the canonical control plane/Store validation boundary.
- Component/instance implementation must not pre-empt the next View Model/Data Binding milestone with an incompatible ad-hoc data system.
